#defmodule PorcelainTest.MiscTest do
#  use ExUnit.Case
#
#  alias Porcelain, as: P
#
#  test "shplit command" do
#    assert P.shplit("this is a   command") == {"this", ["is", "a", "command"]}
#  end
#end
